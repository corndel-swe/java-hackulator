package com.corndel.hackulator;

public class Average {
  /** Divides the numbers. E.g. divide(15, 3) => 5 */
  static int average(int x, int y) {
    // TODO: Remove brackets
    return (x + y) / 2;
  }
}
